# نظام إدارة المطاعم — Restaurant System

نظام ويب متكامل لإدارة المطاعم والطلبات مع واجهة حديثة وبنية قابلة للنشر.

**الحالة:** قيد التطوير • **النسخة:** 0.1.0

---

## نظرة سريعة
منصة ويب حديثة لإدارة المطاعم، القوائم، والسلات، مع لوحة تحكم لمالكي المطاعم وتحديثات طلبات في الوقت الحقيقي.

**الحالة:** قيد التطوير • **النسخة:** 0.1.0

---

## ✨ الميزات الرئيسية

### للعملاء
- تصفح المطاعم وقوائم الطعام بسلاسة
- إضافة عناصر إلى السلة وإنشاء الطلبات
- متابعة الطلبات وتاريخ الطلبات

### لأصحاب المطاعم
- إدارة المطاعم والقوائم والفئات والأطباق
- استقبال وإدارة الطلبات وتحديث الحالات في الوقت الحقيقي
- لوحة تحكم تعرض إحصاءات وطلبات قيد التنفيذ

### عام
- نظام مصادقة وصلاحيات (Customer / Restaurant Owner)
- واجهة مستجيبة مع دعم الوضع الداكن
- تكامل مع Supabase للبيانات والـ realtime

---

## 🛠️ التكنولوجيا

### الواجهة
- Next.js 16 (App Router) • React 19 • TypeScript
- Tailwind CSS • Framer Motion • MUI / Lucide Icons

### إدارة الحالة والبيانات
- React Query (TanStack Query) • Zustand • React Hook Form • Zod

### الباك-إند والبيانات
- Next.js API Routes • Supabase (Postgres) • JWT / bcrypt

---

## 📁 بنية المشروع (مختصر)

- `src/app/` — صفحات التطبيق وطرق الـ API
- `src/features/` — منطق الأعمال، hooks وmutations
- `src/shared/` — عموميات: عميل Supabase، الأدوات والمكتبات المشتركة
- `src/widgets/` — مكونات مركبة وقابلة لإعادة الاستخدام

---

## 🚀 تشغيل محلي — Quick Start

### المتطلبات
- Node.js 18+ وnpm/yarn/pnpm
- حساب Supabase وملف متغيّرات بيئية مُعد

### خطوات سريعة

1. تثبيت الحزم:

```bash
npm install
```

2. إنشاء `.env.local` (انسخ من `.env.example`) واملأ مفاتيح Supabase وJWT وبيانات أخرى.

3. تشغيل بيئة التطوير:

```bash
npm run dev
```

افتح المتصفح عند http://localhost:3000

---

## 🧭 أوامر مفيدة

```bash
npm run dev    # تشغيل بيئة التطوير
npm run build  # بناء لإنتاج
npm run start  # تشغيل الخادم في الإنتاج
npm run lint   # فحص الكود
```

---

## 🔒 أمن وحماية (موجز)

- كلمات مرور مشفرة بـ `bcrypt`
- JWT للتوثيق + HTTP-only cookies
- تحقق من المدخلات باستخدام `Zod`
- حماية مسارات عبر middleware

---

## 📦 واجهات برمجة التطبيقات (مختصر)

- `POST /api/auth/signup` — تسجيل مستخدم
- `POST /api/auth/login` — تسجيل الدخول
- `POST /api/auth/logout` — تسجيل الخروج
- `GET /api/customer/restaurants` — قائمة المطاعم
- `GET /api/customer/menu/[id]` — قائمة مطعم
- `POST /api/customer/orders` — إنشاء طلب
- `GET /api/admin/orders` — إدارة الطلبات (Owner)

لمزيد من التفاصيل راجع مجلد `src/app/api/`.

---

## ☁️ النشر

ينصح باستخدام Vercel أو أي مزود يدعم تطبيقات Next.js. تأكد من ضبط متغيّرات البيئة في لوحة التحكم.

---

## 🤝 المساهمة

- افتح Issue أو أرسل PR. اتبع قواعد الـ lint والـ formatting قبل الدمج.

---

## 📄 الترخيص

مرخّص بموجب ترخيص MIT (راجع `LICENSE`).

---

## 👥 فريق العمل

- اسرد أسماء المطورين أو رابط إلى ملف CONTRIBUTORS حسب الحاجة.

---

إذا رغبت، أستطيع إضافة نسخة إنجليزية أو تلخيص أقصر جداً للملف README. 

| الطبقة | الوصف | الموقع |
|--------|--------|--------|
| **Presentation** | صفحات الويب والمكونات البصرية | `src/app/`, `src/widgets/` |
| **Business Logic** | منطق الأعمال والـ Hooks | `src/features/` |
| **Data Layer** | API Routes والتفاعل مع قاعدة البيانات | `src/app/api/` |
| **Shared** | الأدوات والمساعدات المشتركة | `src/shared/` |

---

## 📦 المكتبات والتقنيات المستخدمة

### تقنيات أساسية:

| التقنية | الإصدار | الاستخدام |
|---------|---------|----------|
| **Next.js** | 16.1.1 | إطار عمل React الكامل مع SSR |
| **React** | 19.2.3 | مكتبة واجهات المستخدم |
| **TypeScript** | 5.x | لغة البرمجة الرئيسية |
| **Tailwind CSS** | 4.1.18 | تصميم وتنسيق CSS |

### مكتبات الواجهة (UI):

| المكتبة | الإصدار | الاستخدام |
|--------|---------|----------|
| **Material-UI (@mui)** | 7.3.6 | مكونات UI احترافية |
| **Emotion** | 11.14.x | CSS-in-JS للتنسيق |
| **Framer Motion** | 12.24.11 | الرسوم المتحركة |
| **Lucide React** | 0.562.0 | أيقونات عالية الجودة |
| **Tabler Icons** | 3.36.1 | مجموعة أيقونات إضافية |
| **Sonner** | 2.0.7 | إشعارات Toast |

### إدارة البيانات والحالة:

| المكتبة | الإصدار | الاستخدام |
|--------|---------|----------|
| **React Query** | 5.90.16 | إدارة البيانات والـ caching |
| **Zustand** | 5.0.10 | إدارة الحالة (Cart) |
| **React Hook Form** | 7.70.0 | إدارة النماذج |
| **Zod** | 4.3.5 | التحقق من صحة البيانات (Validation) |

### المصادقة وقاعدة البيانات:

| المكتبة | الإصدار | الاستخدام |
|--------|---------|----------|
| **Supabase** | 2.89.0 | قاعدة البيانات والتوثيق الفوري |
| **jsonwebtoken** | 9.0.3 | إنشاء والتحقق من JWT tokens |
| **jose** | 6.1.3 | معالجة آمنة للـ JWT |
| **bcrypt** | 6.0.0 | تشفير كلمات المرور |
| **bcryptjs** | 3.0.3 | بديل بدون معتمديات إضافية |

### الأدوات الإضافية:

| المكتبة | الإصدار | الاستخدام |
|--------|---------|----------|
| **next-intl** | 4.7.0 | دعم التعددية اللغوية |
| **next-themes** | 0.4.6 | تبديل المواضيع (Light/Dark) |
| **dayjs** | 1.11.19 | معالجة التواريخ والأوقات |
| **@google/generative-ai** | 0.24.1 | تكامل AI/Gemini |

### أدوات التطوير:

| الأداة | الإصدار | الاستخدام |
|--------|---------|----------|
| **ESLint** | 9.x | فحص جودة الكود |
| **PostCSS** | 8.5.6 | معالجة CSS المتقدمة |
| **Autoprefixer** | 10.4.23 | إضافة البادئات التلقائية للـ CSS |
| **Babel React Compiler** | 1.0.0 | تحسين أداء React |

---

## 🗂️ بنية المشروع

```
restaurant-system/
│
├── src/
│   ├── app/                              # التطبيق الرئيسي
│   │   ├── layout.tsx                    # Layout الجذر
│   │   ├── (auth)/                       # صفحات المصادقة
│   │   │   ├── login/
│   │   │   └── signUp/
│   │   ├── (main-layout)/                # الصفحة الرئيسية
│   │   ├── (shared-layout)/              # Layout مشترك للـ Admin والـ Customer
│   │   │   ├── (admin)/
│   │   │   │   └── orders/
│   │   │   ├── (customer)/
│   │   │   │   ├── cart/
│   │   │   │   ├── checkout/
│   │   │   │   └── order/
│   │   │   └── (shared-pages)/
│   │   │       ├── dashboard/
│   │   │       ├── menu/
│   │   │       └── restaurantDetails/
│   │   ├── api/                          # API Routes
│   │   │   ├── admin/                    # API الـ Admin
│   │   │   │   ├── menu/
│   │   │   │   ├── orders/
│   │   │   │   └── restaurants/
│   │   │   ├── auth/                     # API المصادقة
│   │   │   │   ├── login/
│   │   │   │   ├── logout/
│   │   │   │   ├── me/
│   │   │   │   ├── set-role/
│   │   │   │   └── signup/
│   │   │   └── customer/                 # API العملاء
│   │   │       ├── menu/
│   │   │       ├── orders/
│   │   │       └── restaurants/
│   │   ├── providers/                    # React Context & Providers
│   │   │   ├── QueryProvider.tsx
│   │   │   ├── RestaurantContext.tsx
│   │   │   └── ThemeProvider.tsx
│   │   └── styles/
│   │       └── globals.css
│   │
│   ├── features/                         # منطق الأعمال والـ Hooks
│   │   ├── (admin)/
│   │   │   ├── menu/
│   │   │   │   ├── categories/
│   │   │   │   ├── getMenu/
│   │   │   │   ├── menu_items/
│   │   │   │   └── ui/
│   │   │   ├── order/
│   │   │   │   ├── getOrder/
│   │   │   │   └── realTime/
│   │   │   └── restaurant/
│   │   │       ├── delete-restaurant/
│   │   │       ├── get-restaurants/
│   │   │       └── mutations-restaurant/
│   │   ├── (customer)/
│   │   │   ├── cart/
│   │   │   │   ├── api/
│   │   │   │   ├── libs/
│   │   │   │   ├── model/
│   │   │   │   └── ui/
│   │   │   ├── get-restaurants/
│   │   │   │   ├── api/
│   │   │   │   └── libs/
│   │   │   ├── menu/
│   │   │   │   ├── get-menu/
│   │   │   │   └── libs/
│   │   │   └── order/
│   │   │       ├── getOrder/
│   │   │       └── postOrder/
│   │   └── user/                         # منطق المستخدم
│   │       ├── api/
│   │       │   ├── use-login.ts
│   │       │   ├── use-logout.ts
│   │       │   ├── use-me.ts
│   │       │   └── use-signup.ts
│   │       ├── libs/
│   │       │   ├── login.schema.ts
│   │       │   └── signup.schema.ts
│   │       ├── model/
│   │       │   └── type.ts
│   │       └── ui/
│   │           ├── FoodBackground.tsx
│   │           ├── LoginForm.tsx
│   │           └── SignupForm.tsx
│   │
│   ├── shared/                           # أدوات ومساعدات مشتركة
│   │   ├── api/
│   │   │   ├── supabaseClient.ts
│   │   │   └── supabaseRealTime.ts
│   │   ├── config/
│   │   │   ├── sidebar.config.ts
│   │   │   ├── theme.ts
│   │   │   └── useSearchData.ts
│   │   ├── keys/
│   │   │   └── query-keys.ts
│   │   ├── libs/
│   │   │   └── auth/
│   │   │       ├── auth-file.ts
│   │   │       ├── cookies.ts
│   │   │       ├── getCurrentUser.ts
│   │   │       ├── jwt.ts
│   │   │       ├── password-hash.ts
│   │   │       └── verifyRestaurantOwner.ts
│   │   ├── notifications/
│   │   │   └── orderNotifications.ts
│   │   └── ui/
│   │       ├── Loader.tsx
│   │       ├── Card/
│   │       │   └── AppCard.tsx
│   │       └── Skeletons/
│   │           ├── CartSkeleton.tsx
│   │           ├── LandingPageSkeleton.tsx
│   │           ├── MenuManagementSkeleton.tsx
│   │           └── OrdersAdminSkeleton.tsx
│   │
│   └── widgets/                          # مكونات واجهة كبيرة
│       ├── (admin)/
│       │   ├── dashboard/
│       │   ├── menu/
│       │   ├── orders/
│       │   └── restaurants/
│       ├── (customer)/
│       │   ├── cart/
│       │   ├── homePage/
│       │   └── order/
│       └── header/
│           └── Header.tsx
│
├── public/
│   └── images/                           # الصور الثابتة
│
├── package.json
├── next.config.ts
├── tsconfig.json
├── postcss.config.mjs
├── eslint.config.mjs
├── middleware.ts
└── README.md
```

---

## 🔐 نظام المصادقة والتفويض

### طرق المصادقة:

#### 1. **التسجيل (Signup)**
```
POST /api/auth/signup
{
  "name": "احمد",
  "email": "ahmed@example.com",
  "password": "secure_password",
  "role": "customer" | "restaurant_owner"
}
```

**العملية:**
1. التحقق من صحة البيانات باستخدام Zod
2. التحقق من عدم وجود البريد الإلكتروني مسبقاً
3. تشفير كلمة المرور باستخدام bcrypt
4. إنشاء سجل المستخدم في قاعدة البيانات
5. إنشاء JWT token
6. حفظ التوكن في HTTP-only cookie

#### 2. **تسجيل الدخول (Login)**
```
POST /api/auth/login
{
  "email": "ahmed@example.com",
  "password": "secure_password"
}
```

**العملية:**
1. البحث عن المستخدم باستخدام البريد الإلكتروني
2. التحقق من كلمة المرور مقابل الـ hash المخزن
3. إنشاء JWT token
4. حفظ التوكن في HTTP-only cookie

#### 3. **الخروج (Logout)**
```
POST /api/auth/logout
```

**العملية:**
1. حذف التوكن من الـ cookie

### الأدوار والصلاحيات:

#### مستويات الوصول:

```
┌─────────────┬──────────────┬─────────────────┐
│   الصفحة    │   العام      │   المصادقة      │
├─────────────┼──────────────┼─────────────────┤
│ "/"         │ ✅ عام      │ ✅ مصرح         │
│ "/login"    │ ✅ عام      │ ✅ مصرح         │
│ "/register" │ ✅ عام      │ ✅ مصرح         │
├─────────────┼──────────────┼─────────────────┤
│ /dashboard  │ ❌ مرفوض    │ ✅ مصرح للجميع  │
│ /menu       │ ❌ مرفوض    │ ✅ مصرح للجميع  │
│ /orders     │ ❌ مرفوض    │ ✅ Owner فقط    │
├─────────────┼──────────────┼─────────────────┤
│ /owner/*    │ ❌ مرفوض    │ ✅ Owner فقط    │
│ /customer/* │ ❌ مرفوض    │ ✅ Customer فقط│
└─────────────┴──────────────┴─────────────────┘
```

#### الأدوار:

| الدور | الوصف | الصلاحيات |
|------|--------|----------|
| **CUSTOMER** | عميل عادي | تصفح المطاعم، إنشاء طلبات، تتبع الطلبات |
| **RESTAURANT_OWNER** | مالك مطعم | إدارة المطعم، القائمة، الطلبات |

### دالة التحقق الأمنية:

#### `verifyRestaurantOwner(restaurantId, userId)`

**الغرض:** التأكد من أن المستخدم الحالي هو مالك المطعم المحدد

**مثال:**
```typescript
export async function verifyRestaurantOwner(restaurantId: string, userId: string): Promise<boolean> {
  const restaurant = await db.query(
    `SELECT owner_id FROM restaurants WHERE id = $1`,
    [restaurantId]
  );
  
  return restaurant.owner_id === userId;
}
```

**الاستخدام في:**
- ✅ تحديث/حذف المطعم
- ✅ إنشاء/تحديث/حذف الفئات
- ✅ إنشاء/تحديث/حذف الأطباق
- ✅ تحديث حالة الطلبات (من جانب المالك)

### ملف JWT:

**المكان:** `src/shared/libs/auth/jwt.ts`

**الوظائف الرئيسية:**
- `signToken(payload)` - إنشاء token جديد
- `verifyToken(token)` - التحقق من صحة token
- `refreshToken(token)` - تجديد token منتهي الصلاحية

**محتوى Payload:**
```typescript
type JWTPayload = {
  sub: string;        // معرف المستخدم (ID)
  email: string;      // البريد الإلكتروني
  name: string;       // اسم المستخدم
  role: string;       // الدور (CUSTOMER أو RESTAURANT_OWNER)
  iat: number;        // وقت الإنشاء
  exp: number;        // وقت الانتهاء
};
```

---

## 👨‍💼 وظائف مالك المطعم (Admin)

### 1. إدارة المطاعم

#### إنشاء مطعم:
```typescript
POST /api/admin/restaurants
{
  "name": "مطعم الشرق",
  "description": "مطعم فاخر للمأكولات الشرقية",
  "city": "الرياض",
  "country": "السعودية",
  "image": "url_image"
}
```

#### الحصول على مطاعم المالك:
```
GET /api/admin/restaurants
```

#### تحديث مطعم:
```
PUT /api/admin/restaurants/{restaurantId}
```

#### حذف مطعم:
```
DELETE /api/admin/restaurants/{restaurantId}
```

### 2. إدارة القائمة والأطباق

#### الفئات (Categories):
- ✅ إنشاء فئة جديدة
- ✅ تحديث فئة
- ✅ حذف فئة
- ✅ الحصول على جميع فئات المطعم

#### الأطباق (Menu Items):
- ✅ إضافة طبق جديد
- ✅ تحديث معلومات الطبق
- ✅ حذف طبق
- ✅ تصفية الأطباق حسب الفئة

### 3. إدارة الطلبات

#### الحصول على الطلبات:
```
GET /api/admin/orders?restaurantId={id}
```

#### تحديث حالة الطلب:
```
PUT /api/admin/orders/{orderId}/status
{
  "status": "pending" | "confirmed" | "preparing" | "ready" | "delivered" | "cancelled"
}
```

#### الأنماط المتاحة:
| الحالة | الوصف |
|--------|--------|
| **pending** | انتظار القبول |
| **confirmed** | تم قبول الطلب |
| **preparing** | جاري التحضير |
| **ready** | جاهز للتسليم |
| **delivered** | تم التسليم |
| **cancelled** | تم الإلغاء |

---

## 👤 وظائف العميل (Customer)

### 1. الاستكشاف والبحث

#### الحصول على جميع المطاعم:
```
GET /api/customer/restaurants
```

#### البحث عن مطعم:
```
GET /api/customer/restaurants?city={city}&search={query}
```

#### تفاصيل مطعم:
```
GET /api/customer/restaurants/{restaurantId}
```

### 2. تصفح القوائم

#### الحصول على قائمة المطعم:
```
GET /api/customer/menu?restaurantId={id}
```

#### البحث عن أطباق:
```
GET /api/customer/menu?search={query}
```

### 3. إنشاء الطلبات

#### إنشاء طلب جديد:
```
POST /api/customer/orders
{
  "restaurantId": "123",
  "items": [
    { "itemId": "item1", "quantity": 2 },
    { "itemId": "item2", "quantity": 1 }
  ],
  "totalAmount": 45.99,
  "specialInstructions": "بدون بصل"
}
```

#### الحصول على طلباتي:
```
GET /api/customer/orders
```

#### تفاصيل الطلب:
```
GET /api/customer/orders/{orderId}
```

#### إلغاء الطلب:
```
DELETE /api/customer/orders/{orderId}
```

---

## 🛒 نظام السلة

### تقنية التخزين:
- **State Management:** Zustand (متجر محلي في الذاكرة)
- **التخزين الدائم:** LocalStorage (يدعمه تلقائياً)
- **المشاركة:** عبر Context و Zustand hooks

### Zustand Store:

**الملف:** `src/features/(customer)/cart/model/useCartStore.ts`

```typescript
interface CartItem {
  id: string;
  name: string;
  price: number;
  quantity: number;
  restaurantId: string;
  image?: string;
}

interface CartStore {
  items: CartItem[];
  addItem(item: CartItem): void;
  removeItem(itemId: string): void;
  updateQuantity(itemId: string, quantity: number): void;
  clearCart(): void;
  getTotalPrice(): number;
  getItemCount(): number;
}
```

### العمليات الأساسية:

#### إضافة عنصر:
```typescript
const { addItem } = useCartStore();
addItem({
  id: "item1",
  name: "برجر",
  price: 25,
  quantity: 2,
  restaurantId: "rest1"
});
```

#### حذف عنصر:
```typescript
const { removeItem } = useCartStore();
removeItem("item1");
```

#### تحديث الكمية:
```typescript
const { updateQuantity } = useCartStore();
updateQuantity("item1", 5);
```

#### حساب الإجمالي:
```typescript
const { getTotalPrice } = useCartStore();
const total = getTotalPrice(); // 125.50
```

### القيود:
- ✅ عنصر واحد فقط من مطعم واحد في نفس الوقت
- ✅ تنبيهات عند محاولة إضافة من مطعم آخر
- ✅ حذف تلقائي للسلة عند اختيار مطعم جديد

---

## 📦 نظام الطلبات

### دورة حياة الطلب:

```
┌──────────┐
│  PENDING │ (انتظار قبول المالك)
└────┬─────┘
     │
     ▼
┌───────────┐
│ CONFIRMED │ (تم قبول الطلب)
└────┬──────┘
     │
     ▼
┌──────────────┐
│  PREPARING   │ (جاري التحضير)
└────┬─────────┘
     │
     ▼
┌────────┐
│ READY  │ (جاهز)
└────┬───┘
     │
     ▼
┌───────────┐
│ DELIVERED │ (تم التسليم)
└───────────┘
```

### تحديثات الوقت الفعلي:

**التقنية:** Supabase Realtime Subscriptions

**الملف:** `src/shared/api/supabaseRealTime.ts`

```typescript
// الاستماع لتحديثات الطلبات
subscribeToOrders(restaurantId, (updatedOrder) => {
  console.log("تحديث جديد:", updatedOrder);
  // تحديث الواجهة تلقائياً
});
```

### الإشعارات:

**الملف:** `src/shared/notifications/orderNotifications.ts`

**الإشعارات المتاحة:**
- 📬 طلب جديد وارد
- ✅ تم قبول الطلب
- 🍳 جاري التحضير
- 🎉 الطلب جاهز
- 📦 تم التسليم

---

## ⚛️ React Query Hooks

### React Query Setup:

**الملف:** `src/app/providers/QueryProvider.tsx`

```typescript
export default function QueryClientProvider({ children }) {
  const queryClient = new QueryClient({
    defaultOptions: {
      queries: {
        staleTime: 1000 * 60 * 5, // 5 دقائق
        gcTime: 1000 * 60 * 10,   // 10 دقائق
      },
    },
  });

  return (
    <QueryClientProvider client={queryClient}>
      {children}
      <ReactQueryDevtools initialIsOpen={false} />
    </QueryClientProvider>
  );
}
```

### Query Keys:

**الملف:** `src/shared/keys/query-keys.ts`

```typescript
export const queryKeys = {
  user: {
    all: ["user"] as const,
    me: () => ["user", "me"] as const,
  },
  
  restaurants: {
    all: ["restaurants"] as const,
    list: (filters?: { city?: string }) =>
      ["restaurants", filters || "all"] as const,
    details: (id: string) => ["restaurants", id] as const,
  },
  
  cart: {
    all: ["cart"] as const,
  },
  
  orders: {
    all: ["orders"] as const,
    details: (orderId: string) => ["orders", orderId] as const,
  },
};
```

### Hooks الرئيسية:

#### للعملاء:

| Hook | الاستخدام |
|------|----------|
| `useRestaurantsForCustomer()` | الحصول على قائمة المطاعم |
| `useMenu(restaurantId)` | الحصول على قائمة المطعم |
| `useGetOrders()` | الحصول على طلباتي |
| `useCart()` | إدارة السلة |
| `usePlaceOrder()` | إنشاء طلب جديد |

#### لمالكي المطاعم:

| Hook | الاستخدام |
|------|----------|
| `useRestaurants()` | الحصول على مطاعمي |
| `useAddRestaurant()` | إضافة مطعم جديد |
| `useUpdateRestaurant()` | تحديث بيانات المطعم |
| `useDeleteRestaurant()` | حذف المطعم |
| `useCategories(restaurantId)` | إدارة الفئات |
| `useMenuItems(categoryId)` | إدارة الأطباق |
| `useOrders(restaurantId)` | الحصول على طلبات المطعم |
| `useUpdateOrderStatus()` | تحديث حالة الطلب |

#### للمستخدم:

| Hook | الاستخدام |
|------|----------|
| `useMe()` | بيانات المستخدم الحالي |
| `useLogin()` | تسجيل الدخول |
| `useSignup()` | التسجيل |
| `useLogout()` | تسجيل الخروج |

### مثال للاستخدام:

```typescript
import { useRestaurantsForCustomer } from '@/features/(customer)/get-restaurants/api/useRestaurants';

export function RestaurantList() {
  const { data: restaurants, isLoading, error } = useRestaurantsForCustomer();

  if (isLoading) return <Skeleton />;
  if (error) return <div>خطأ: {error.message}</div>;

  return (
    <div>
      {restaurants?.map(restaurant => (
        <RestaurantCard key={restaurant.id} restaurant={restaurant} />
      ))}
    </div>
  );
}
```

---

## 🗄️ قاعدة البيانات

### الخادم المستخدم:
**Supabase** (PostgreSQL)

### الجداول الرئيسية:

#### جدول المستخدمين (Users)
```sql
CREATE TABLE users (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  email VARCHAR UNIQUE NOT NULL,
  name VARCHAR NOT NULL,
  password_hash VARCHAR NOT NULL,
  role VARCHAR DEFAULT 'customer', -- 'customer' | 'restaurant_owner'
  has_restaurant BOOLEAN DEFAULT false,
  created_at TIMESTAMP DEFAULT now(),
  updated_at TIMESTAMP DEFAULT now()
);
```

#### جدول المطاعم (Restaurants)
```sql
CREATE TABLE restaurants (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  owner_id UUID NOT NULL REFERENCES users(id),
  name VARCHAR NOT NULL,
  description TEXT,
  city VARCHAR,
  country VARCHAR,
  image VARCHAR,
  rating DECIMAL DEFAULT 0,
  created_at TIMESTAMP DEFAULT now(),
  updated_at TIMESTAMP DEFAULT now()
);
```

#### جدول الفئات (Categories)
```sql
CREATE TABLE categories (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  restaurant_id UUID NOT NULL REFERENCES restaurants(id),
  name VARCHAR NOT NULL,
  description TEXT,
  created_at TIMESTAMP DEFAULT now()
);
```

#### جدول الأطباق (Menu Items)
```sql
CREATE TABLE menu_items (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  restaurant_id UUID NOT NULL REFERENCES restaurants(id),
  category_id UUID REFERENCES categories(id),
  name VARCHAR NOT NULL,
  description TEXT,
  price DECIMAL NOT NULL,
  image VARCHAR,
  is_available BOOLEAN DEFAULT true,
  created_at TIMESTAMP DEFAULT now(),
  updated_at TIMESTAMP DEFAULT now()
);
```

#### جدول الطلبات (Orders)
```sql
CREATE TABLE orders (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  customer_id UUID NOT NULL REFERENCES users(id),
  restaurant_id UUID NOT NULL REFERENCES restaurants(id),
  status VARCHAR DEFAULT 'pending', -- pending|confirmed|preparing|ready|delivered|cancelled
  total_amount DECIMAL NOT NULL,
  special_instructions TEXT,
  created_at TIMESTAMP DEFAULT now(),
  updated_at TIMESTAMP DEFAULT now()
);
```

#### جدول تفاصيل الطلب (Order Items)
```sql
CREATE TABLE order_items (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  order_id UUID NOT NULL REFERENCES orders(id),
  menu_item_id UUID NOT NULL REFERENCES menu_items(id),
  quantity INTEGER NOT NULL,
  price DECIMAL NOT NULL,
  created_at TIMESTAMP DEFAULT now()
);
```

### الاتصال بـ Supabase:

**الملف:** `src/shared/api/supabaseClient.ts`

```typescript
import { createClient } from '@supabase/supabase-js';

const supabase = createClient(
  process.env.NEXT_PUBLIC_SUPABASE_URL!,
  process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
);

export default supabase;
```

---

## 🎨 نظام المواضيع والتصميم

### نظام الألوان:

**الملف:** `src/shared/config/theme.ts`

#### الألوان الرئيسية:

| المكون | الفاتح | الغامق |
|--------|--------|--------|
| **Primary** | #4E342E (بني خشبي) | #D7CCC8 (كريمي) |
| **Secondary** | #A1887F (بيج) | #8D6E63 (بني فاتح) |
| **Background** | #FDFBF7 (أبيض دافئ) | #1A1614 (بني غامق) |
| **Text Primary** | #3E2723 (بني غامق) | #F5F5F5 (رمادي فاتح) |

### التبديل بين المواضيع:

```typescript
import { useColorMode } from "@/app/providers/ThemeProvider";

export function ThemeToggle() {
  const { toggleColorMode } = useColorMode();

  return (
    <button onClick={toggleColorMode}>
      تبديل المظهر
    </button>
  );
}
```

### الخط المستخدم:
- **الخط الرئيسي:** Cairo (عربي جميل)
- **البديل:** sans-serif

### Border Radius:
- **الافتراضي:** 12px (زوايا ناعمة)

### الظلال:
- استخدام alpha (شفافية) على الألوان الأساسية
- ظلال دقيقة بدون المبالغة

---

## 📱 الاستجابة والعروض

### نقاط التفتيش (Breakpoints):

| الحد | العرض | الجهاز |
|-----|--------|---------|
| **xs** | < 600px | الهاتف الصغير |
| **sm** | 600-960px | الهاتف الكبير |
| **md** | 960-1264px | الجهاز اللوحي |
| **lg** | 1264-1904px | الشاشة الكبيرة |
| **xl** | > 1904px | الشاشة الكبيرة جداً |

### استخدام الاستجابة:

```typescript
<Box
  sx={{
    fontSize: { xs: "1rem", md: "1.5rem" },
    padding: { xs: 1, md: 4 },
    display: { xs: "none", md: "block" },
  }}
>
  محتوى استجابي
</Box>
```

### الصفحات الرئيسية والعروض:

#### الصفحة الرئيسية (Landing Page)
- Hero Section - شريط ترحيب
- How It Works - خطوات الاستخدام
- CTA Section - نداء للتحرك
- Role Selection - اختيار الدور

#### لوحة التحكم (Dashboard)
- لـ Admin: عرض المطاعم والإحصائيات
- لـ Customer: عرض الطلبات والمطاعم

#### إدارة القائمة (Menu Management)
- عرض الفئات والأطباق
- إضافة/تحديث/حذف الأطباق

#### الطلبات (Orders)
- لـ Customer: قائمة طلباتي
- لـ Admin: قائمة طلبات المطعم مع الحالات

---

## 🔒 مبادئ الأمان

### 1️⃣ المصادقة والتفويض:
- ✅ JWT tokens في HTTP-only cookies
- ✅ التحقق من الهوية على الخادم في كل طلب
- ✅ لا تثق أبداً ببيانات المستخدم من الواجهة الأمامية
- ✅ التحقق من الأدوار على الخادم

### 2️⃣ معالجة كلمات المرور:
- ✅ تشفير باستخدام bcrypt مع salt rounds = 10
- ✅ عدم تخزين كلمة المرور النصية
- ✅ عدم إعادة كلمة المرور في الاستجابات

### 3️⃣ التحقق من البيانات:
- ✅ التحقق على الخادم من جميع المدخلات
- ✅ استخدام Zod للتحقق من الصيغة
- ✅ فحص طول وصيغة البريد الإلكتروني
- ✅ التحقق من القيم المنطقية

### 4️⃣ التحكم في الوصول:
- ✅ التحقق من ملكية المورد (verifyRestaurantOwner)
- ✅ عدم كشف معرفات الموارد الخاصة بالآخرين
- ✅ الفحص دائماً على الخادم قبل التحديث/الحذف

### 5️⃣ حماية البيانات:
- ✅ استخدام HTTPS في الإنتاج
- ✅ عدم تسجيل البيانات الحساسة
- ✅ تنظيف البيانات الشخصية

### 6️⃣ معالجة الأخطاء:
- ✅ عدم إرجاع رسائل خطأ مفصلة للعملاء
- ✅ تسجيل الأخطاء على الخادم فقط
- ✅ إرجاع رسائل عامة آمنة

### 7️⃣ حماية API:
- ✅ التحقق من المصادقة لكل الطلبات المحمية
- ✅ التحقق من الأدوار المناسبة
- ✅ حد معقول لمعدل الطلبات (Rate Limiting) - للمستقبل

---

## 🚀 كيفية البدء

### المتطلبات المسبقة:
- Node.js 18.x أو أحدث
- npm أو yarn
- Supabase account
- متغيرات البيئة مكونة

### التثبيت:

1. **استنساخ المشروع:**
```bash
git clone <repository-url>
cd restaurant-system
```

2. **تثبيت المكتبات:**
```bash
npm install
# أو
yarn install
```

3. **إعداد متغيرات البيئة:**

أنشئ ملف `.env.local` بالجذر:

```env
# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key

# JWT
JWT_SECRET=your_jwt_secret_key

# اختياري: Google AI
NEXT_PUBLIC_GOOGLE_API_KEY=your_google_api_key
```

4. **تشغيل خادم التطوير:**
```bash
npm run dev
# أو
yarn dev
```

5. **فتح التطبيق:**
اذهب إلى `http://localhost:3000`

### إنشاء الحساب الأول:
1. انقر على "Sign Up"
2. اختر دورك (Customer أو Restaurant Owner)
3. أدخل بيانات تسجيل
4. ستتم إعادة التوجيه إلى لوحة التحكم

---

## 🛠️ الأوامر المتاحة

### أوامر التطوير:

```bash
# تشغيل خادم التطوير
npm run dev

# بناء الإنتاج
npm run build

# تشغيل النسخة المبنية
npm start

# فحص الكود بـ ESLint
npm run lint
```

### أوامر إضافية:

```bash
# مسح ملف التخزين المؤقت
rm -rf .next

# تثبيت المكتبات من جديد
rm -rf node_modules package-lock.json
npm install
```

---

## 📚 المراجع والتوثيق

### الموارد الرسمية:
- [Next.js Documentation](https://nextjs.org/docs)
- [React Documentation](https://react.dev)
- [Material-UI Documentation](https://mui.com)
- [Supabase Documentation](https://supabase.com/docs)
- [React Query Documentation](https://tanstack.com/query)
- [Zustand Documentation](https://github.com/pmndrs/zustand)

### المشاريع ذات الصلة:
- Frontend: هذا المشروع
- Backend: متدمج مع Next.js API Routes

---

## 📝 ملاحظات مهمة

### تطوير جديد:
- اتبع نمط المجلدات الموجودة
- استخدم TypeScript دائماً
- أضف tests للميزات الجديدة
- وثق التغييرات في هذا README

### أمان:
- ❌ لا تشارك JWT_SECRET
- ❌ لا تخزن كلمات المرور بصيغة نصية
- ✅ تحقق دائماً على الخادم
- ✅ استخدم HTTPS في الإنتاج

### الأداء:
- استخدم React Query للـ caching الذكي
- استخدم Code Splitting تلقائياً من Next.js
- قلل حجم الصور
- استخدم CDN للملفات الثابتة

---

## 👥 المساهمة

يرجى الالتزام بـ:
1. الكود النظيف والمنظم
2. التعليقات الواضحة
3. الاختبار الشامل
4. توثيق التغييرات

---

## 📄 الترخيص

هذا المشروع مرخص تحت [LICENSE NAME]

---

## 📧 التواصل والدعم

للأسئلة والدعم، يرجى:
- فتح Issue في GitHub
- إرسال بريد إلكتروني إلى الفريق
- المشاركة في النقاشات

---

**تم آخر تحديث:** 29 يناير 2026  
**الإصدار:** 0.1.0  
**الحالة:** قيد التطوير 🚀

